# Suivi technique EtR-core

Dernière mise à jour : 2026-08-02

Ce document est une barrière de gouvernance du dépôt. Toute modification fonctionnelle doit mettre à jour son état ou ajouter une ligne. Le suivi consolidé site + application est maintenu dans le dépôt privé `ORYX-PROJETS`.

| ID | Périmètre | Fonction / risque | Priorité | État | Constat | Correction / preuve | Prochaine action |
|---|---|---|---|---|---|---|---|
| ETR-001 | Dashboard local | Code installé hors GitHub sous `/opt/etr/dashboard` | P0 | Corrigé le 2026-07-26 | Service actif mais sources absentes du dépôt | Dashboard, dépendances, unité systemd et tests versionnés sous `dashboard/` | Valider le rapport du runner ARM64 et le rendu physique |
| ETR-002 | Déploiement | Le workflow ne surveillait ni `src/app.py` ni le dashboard | P0 | Corrigé le 2026-07-26 | Un changement applicatif pouvait ne pas être déployé | Filtres, contrôles HTTP et rapport de déploiement étendus | Conserver les contrôles comme conditions de réussite |
| ETR-003 | API locale | API limitée à un pourcentage CPU | P0 | Corrigé et validé physiquement le 2026-08-02 | Aucun contrat de données métier stable | API v1, santé, système, télémétrie normalisée et acquisition réelle ADS1263 alimentent désormais le dashboard sans valeurs simulées | Étendre le registre de points aux futurs capteurs d'humidité et sorties de commande |
| ETR-004 | Sécurité Edge | API Flask de développement exposée sur `0.0.0.0` | P0 | Corrigé le 2026-07-26 | Service joignable sur toutes les interfaces | Gunicorn sur loopback + sandbox systemd | Vérifier les ports après chaque déploiement |
| ETR-005 | Wi-Fi tactile | Mise en service sans PC | P0 | Déployé | Test de déploiement 2026-07-19 réussi | Portail, clavier tactile et reprise automatique contrôlés | Test terrain WPA2/WPA3 et réseau masqué |
| ETR-006 | Écran SPI / kiosque | Reprise au démarrage à froid | P0 | Déployé | Correctif fusionné le 2026-07-18 | Dépendances systemd et anti-blanking testés | Test après coupure électrique prolongée |
| ETR-007 | Écran distant | Le compte client affiche que l'écran distant est indisponible malgré VNC, la session Firebase et `deviceAccess` valides | P0 | Autorisation WebSocket corrigée en PR, preuve Cloud/Edge attendue | La sonde physique lit `deviceAccess/<uid>` en HTTP 200 avec la valeur `etr-core`, mais l'upgrade `/device` renvoie encore HTTP 500. Le serveur utilisait une lecture Firebase Admin distincte dans le gestionnaire WebSocket, puis transformait toute exception non qualifiée en 500 | Le gestionnaire `/device` utilise désormais le jeton Firebase du Raspberry pour relire `deviceAccess/<uid>`, distingue 401/403/503, exige la correspondance exacte avec l'installation, journalise les connexions et déclenche la réparation ARM64 après déploiement | Fusionner, obtenir `/api/health` 1.1.1, la connexion WSS `101`, puis rejouer « Ouvrir l'écran » depuis le compte client vérifié |
| ETR-008 | Firebase | Publication de la télémétrie | P0 | Acquisition locale réelle validée ; publication cloud à contrôler | Bridge actif et appareil enrôlé, mais la preuve conservée porte d'abord sur la lecture locale des quatre canaux | Contrat API v1, fichier atomique et bridge Firebase actifs sur `etr-core` | Vérifier dans l'espace client l'horodatage et les quatre canaux publiés après plusieurs heures |
| ETR-009 | Scalabilité | Passerelle distante mono-instance avec sessions et tickets en mémoire | P1 | À faire | Les sessions appareil/viewer sont locales à une instance Cloud Run | Architecture documentée et `max-instances=1` conservé pour le pilote | Ajouter Redis ou Pub/Sub avant toute montée à plusieurs instances |
| ETR-010 | Secrets | Dépôt public et configuration sensible | P0 | Renforcé | Ancien installateur lançait le bridge Firebase sous `root` | Unité versionnée sous `oryx`, environnement `root:oryx 0640`, états `oryx:oryx 0600`, sandbox systemd et tests de contrat | Vérifier les permissions dans le rapport physique |
| ETR-011 | Enrôlement | Numéro de série + code temporaire seulement documentés, aucun backend | P0 | Implémenté et validé physiquement le 2026-08-02 | Impossible d'associer un EtR neuf sans intervention manuelle | Bootstrap GitHub OIDC réussi, installation `etr-0000dd7429c2` enregistrée et état final `enrolled` sur le Raspberry | Reproduire le parcours autonome sur le Raspberry Pi 3 |
| ETR-012 | Dashboard d'activation | Aucun code visible sur l'écran tactile | P0 | Implémenté et contrôlé sur le Raspberry | L'utilisateur ne pouvait pas finaliser seul une première mise en service | API locale sûre, panneau tactile, expiration visible et suppression du code après enrôlement | Rejouer le parcours sur un appareil neuf non enrôlé |
| ETR-013 | Passerelle Cloud | Le chemin public `/healthz` est intercepté par Google Frontend alors que les routes Express métier répondent | P0 | Alias public versionné 1.1.1 en PR | La sonde publique obtient 400 JSON sur l'enrôlement et 401 JSON sur l'administration, mais une page Google 404 sur `/healthz`; ce chemin ne prouve donc pas la révision Express réellement servie | Ajout de `/api/health` avec service, version, `K_REVISION`, appareils et viewers; le workflow exige désormais version 1.1.1, révision exacte et 100 % du trafic avant de poursuivre | Déployer, exiger `/api/health` 200 sur la révision attendue et publier un rapport `status=success` |
| ETR-014 | Gouvernance | Le bridge, l'enrôlement ou leurs preuves pouvaient être omis | P0 | Corrigé | Pas de contrat de dépôt couvrant la chaîne complète | Tests Python/Node et contrat exigeant backend, services systemd, UI, workflows et rapports | Maintenir cette barrière sur toute PR fonctionnelle |
| ETR-015 | Identité Firebase appareil | `createCustomToken` dépendait de `signBlob`, non autorisé pour l'identité Cloud Run | P0 | Correctif sans `signBlob` déployé et validé | Cloud Run et les routes fonctionnaient, mais `customTokenSigning=false` bloquait l'identité finale | Compte technique déterministe, mot de passe éphémère, claims EtR et session directe ont permis l'état final `enrolled` | Contrôler la rotation automatique du jeton sur une durée supérieure à une heure |
| ETR-016 | Image Docker | Des modules de sécurité présents dans GitHub pouvaient manquer de l'image déployée | P0 | Contrôle renforcé en PR | L'image actuelle n'embarquait ni le nouveau module de santé ni le module d'autorisation appareil tant qu'ils n'étaient pas ajoutés explicitement au Dockerfile | Le Dockerfile copie `health.mjs` et `device-authorization.mjs`; le smoke test vérifie leur présence et `/api/health` après construction | Conserver la liste explicite et le smoke test sur chaque évolution de la passerelle |
| ETR-017 | Compte client | La passerelle pouvait encore accepter un token humain non vérifié si un membership existait | P0 | Corrigé et fusionné | Les règles RTDB et l'interface bloquaient, mais l'autorisation Admin devait appliquer la même condition | Vérificateur central : staff/développeur, appareil `etrDevice`, e-mail vérifié ou ancien compte technique lié à `deviceAccess` | Confirmer avec le compte client réel |
| ETR-018 | Règles RTDB | EtR-core ne contenait pas encore les règles `email_verified` fusionnées côté site et le workflow tentait une auto-attribution IAM | P0 | Fusionné, rapport attendu | Les règles humaines n'étaient pas synchronisées ; la preuve distante ne dépassait pas l'étape IAM | Règles synchronisées, tests détaillés, publication directe par l'identité WIF, relecture et SHA-256 | Exiger `verified=true` dans le rapport post-fusion |
| ETR-019 | Preuve Cloud | Le rapport de déploiement pouvait déclarer `deploy=success` sans prouver la version applicative réellement servie | P0 | Corrigé en PR | Le dernier rapport indique révision et trafic 100 %, mais `verify=failure` et aucune vérification de version; `/healthz` n'était pas un chemin public fiable | Le workflow vérifie `/api/health`, exige `service=etr-remote-gateway`, version 1.1.1 et révision égale à `latestReadyRevisionName`, puis déclenche la réparation Raspberry uniquement après émission de session Firebase réussie | Conserver le rapport avec version, révision, trafic, routes et mode d'autorisation appareil |
| ETR-020 | Compatibilité bridge | Le Raspberry n'acceptait que `customToken` | P0 | Corrigé, déployé et validé | Une session directe sécurisée ne pouvait pas être enregistrée localement | Bridge 3.2 accepte `idToken` + `refreshToken` et le déploiement physique termine avec `enrollment_status=enrolled` | Contrôler le renouvellement après expiration du premier ID token |
| ETR-021 | Configuration Firebase Cloud | Le déploiement de la session directe exigeait un secret `FIREBASE_API_KEY` absent du dépôt EtR-core | P0 | Déployé et validé le 2026-08-02 | La clé Firebase Web est une configuration publique déjà publiée par Firebase Hosting | Lecture de `__/firebase/init.json`, validation du `projectId`, masquage de l'API key et bootstrap réussi dans le rapport physique | Conserver la découverte automatique et le contrôle de projet |
| ETR-022 | Administration globale | Base présente mais aucun accès métier global, claim administrateur ou journal d'audit | P0 | Implémenté, publication Cloud à revalider avec 1.1.1 | Le compte ORYX ne pouvait administrer la flotte que par la console Firebase brute | API `/api/admin/*`, bootstrap par adresse vérifiée, claim `oryxAdmin`, lecture globale, rôles, transfert propriétaire, statut utilisateur, révocation sessions, authentification récente et audit privé | Après déploiement 1.1.1, ouvrir `/etr-admin/` avec le compte ORYX vérifié et contrôler le claim global |
| ETR-023 | Acquisition ADS1263 | Les CAREL et AKO étaient câblés sans driver ni interface ; les fils provisoires des NTC pouvaient être interprétés comme une température | P0 | Déployé et validé physiquement le 2026-08-02 | L'écran occupait SPI0.0/SPI0.1, GPIO17 et maintenait initialement GPIO18 bas ; le portail root perturbait aussi les droits du fichier d'état | SPI0.2/GPIO22, RESET GPIO18 haut, reset logiciel, service non privilégié et écriture atomique validés. Le rapport final détecte l'ADS1263 `chip_id=1`, lit CAREL 1 à 0,5119 V / 0,13 bar et CAREL 2 à 0,5248 V / 0,28 bar. Les deux AKO restent volontairement sans température à 5,0 V avec le statut `reference_resistor_missing_or_probe_open` | Remplacer chaque tortillon bleu par une résistance 10 kΩ 1 %, puis valider la résistance NTC et intégrer uniquement une courbe AKO documentée |
| ETR-024 | Navigation tactile locale | Le kiosque plein écran ne permettait pas de retrouver le bureau Linux sans clavier ; après fermeture, aucun retour tactile garanti vers EtR | P0 | Déployé et validé physiquement | Le Raspberry est utilisé sans clavier et doit rester administrable localement par la souris ou l'écran tactile | Rapport du 2026-08-02 : `job_status=success`, bouton « Bureau Linux » servi, raccourci « Revenir à EtR » présent, bureau SPI actif, kiosque et Chromium actifs après l'aller-retour | Conserver ce test dédié après chaque modification du kiosque ou du bureau |
| ETR-025 | Diagnostic brut ADS1263 | Le rapport d'acquisition masquait l'octet reçu et ne distinguait pas conflit GPIO, mauvais mode SPI, vitesse ou absence physique du HAT | P0 | Validé physiquement le 2026-08-02 | Les premiers essais à zéro provenaient d'un RESET bas et d'erreurs d'exécution, pas d'un défaut démontré du HAT | La sonde finale retourne `probeExitCode=0`, détecte l'ADS1263, lit `ID=0x23`, `POWER=0x11`, `INTERFACE=0x05`, confirme la révision 3 et retrouve une télémétrie `online` avec le service actif | Conserver le mode SPI 1 et cette sonde comme contrôle de régression matériel |
