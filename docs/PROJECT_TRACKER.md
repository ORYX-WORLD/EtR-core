# Suivi technique EtR-core

Dernière mise à jour : 2026-08-03

Ce document est une barrière de gouvernance du dépôt. Toute modification fonctionnelle doit mettre à jour son état ou ajouter une ligne. Le suivi consolidé site + application est maintenu dans le dépôt privé `ORYX-PROJETS`.

| ID | Périmètre | Fonction / risque | Priorité | État | Constat | Correction / preuve | Prochaine action |
|---|---|---|---|---|---|---|---|
| ETR-001 | Dashboard local | Code installé hors GitHub sous `/opt/etr/dashboard` | P0 | Corrigé le 2026-07-26 | Service actif mais sources absentes du dépôt | Dashboard, dépendances, unité systemd et tests versionnés sous `dashboard/` | Valider le rapport du runner ARM64 et le rendu physique |
| ETR-002 | Déploiement | Le workflow ne surveillait ni `src/app.py` ni le dashboard | P0 | Corrigé le 2026-07-26 | Un changement applicatif pouvait ne pas être déployé | Filtres, contrôles HTTP et rapport de déploiement étendus | Conserver les contrôles comme conditions de réussite |
| ETR-003 | API locale | API limitée à un pourcentage CPU | P0 | Corrigé et validé physiquement le 2026-08-02 | Aucun contrat de données métier stable | API v1, santé, système, télémétrie normalisée et acquisition réelle ADS1263 alimentent désormais le dashboard sans valeurs simulées | Étendre le registre de points aux futurs capteurs d'humidité et sorties de commande |
| ETR-004 | Sécurité Edge | API Flask de développement exposée sur `0.0.0.0` | P0 | Corrigé le 2026-07-26 | Service joignable sur toutes les interfaces | Gunicorn sur loopback + sandbox systemd | Vérifier les ports après chaque déploiement |
| ETR-005 | Wi-Fi tactile | Mise en service sans PC | P0 | Déployé | Test de déploiement 2026-07-19 réussi | Portail, clavier tactile et reprise automatique contrôlés | Test terrain WPA2/WPA3, réseau masqué et mauvaise clé |
| ETR-006 | Écran SPI / kiosque | Reprise au démarrage à froid | P0 | Déployé | Correctif fusionné le 2026-07-18 | Dépendances systemd et anti-blanking testés | Test après coupure électrique prolongée |
| ETR-007 | Écran distant | Le compte client affichait que l'écran distant était indisponible malgré VNC, la session Firebase et `deviceAccess` valides | P0 | Validé de bout en bout avec un compte humain vérifié le 2026-08-02 | La révision `etr-remote-gateway-00025-d9r` maintient `devices=1`. Un compte temporaire a reçu et appliqué un vrai message de vérification, obtenu `email_verified=true`, reçu un membership viewer et ouvert l'écran réel | `/api/remote-session` a répondu 200, `/viewer` et noVNC 200, `/client` 101, puis le flux a livré la bannière `RFB 003.008`; le membership et le compte ont été supprimés et `viewers=0` après fermeture | Réaliser le contrôle visuel avec le compte réel d'Emmanuel sur téléphone et vérifier les commandes souris/tactile |
| ETR-008 | Firebase | Publication de la télémétrie | P0 | Acquisition locale réelle validée ; publication cloud à contrôler | Bridge actif et appareil enrôlé, mais la preuve conservée porte d'abord sur la lecture locale des quatre canaux | Contrat API v1, fichier atomique et bridge Firebase actifs sur `etr-core` | Vérifier dans l'espace client l'horodatage et les quatre canaux publiés après plusieurs heures |
| ETR-009 | Scalabilité | Passerelle distante mono-instance avec sessions et tickets en mémoire | P1 | À faire | Les sessions appareil/viewer sont locales à une instance Cloud Run | Architecture documentée et `max-instances=1` conservé pour le pilote | Ajouter Redis ou Pub/Sub avant toute montée à plusieurs instances |
| ETR-010 | Secrets | Dépôt public et configuration sensible | P0 | Renforcé | Ancien installateur lançait le bridge Firebase sous `root` | Unité versionnée sous `oryx`, environnement `root:oryx 0640`, états `oryx:oryx 0600`, sandbox systemd et tests de contrat | Vérifier les permissions dans le rapport physique |
| ETR-011 | Enrôlement | Numéro de série + code temporaire seulement documentés, aucun backend | P0 | Implémenté et validé physiquement le 2026-08-02 | Impossible d'associer un EtR neuf sans intervention manuelle | Bootstrap GitHub OIDC réussi, installation `etr-0000dd7429c2` enregistrée et état final `enrolled` sur le Raspberry | Reproduire le parcours autonome sur le Raspberry Pi 3 |
| ETR-012 | Dashboard d'activation | Aucun code visible sur l'écran tactile | P0 | Implémenté et contrôlé sur le Raspberry | L'utilisateur ne pouvait pas finaliser seul une première mise en service | API locale sûre, panneau tactile, expiration visible et suppression du code après enrôlement | Rejouer le parcours sur un appareil neuf non enrôlé |
| ETR-013 | Passerelle Cloud | Le chemin public `/healthz` est intercepté par Google Frontend alors que les routes Express métier répondent | P0 | Alias public 1.1.1 déployé et validé | `/api/health` répond en 200 sur la révision `etr-remote-gateway-00025-d9r`, avec trafic 100 %, `devices=1` et `viewers=0` après le test | Le workflow contrôle la version et la révision exactes par `/api/health`; `/healthz` reste uniquement le contrôle Docker local | Conserver la preuve version/révision après chaque publication Cloud Run |
| ETR-014 | Gouvernance | Le bridge, l'enrôlement ou leurs preuves pouvaient être omis | P0 | Corrigé | Pas de contrat de dépôt couvrant la chaîne complète | Tests Python/Node et contrat exigeant backend, services systemd, UI, workflows et rapports | Maintenir cette barrière sur toute PR fonctionnelle |
| ETR-015 | Identité Firebase appareil | `createCustomToken` dépendait de `signBlob`, non autorisé pour l'identité Cloud Run | P0 | Correctif sans `signBlob` déployé et validé | Cloud Run et les routes fonctionnaient, mais `customTokenSigning=false` bloquait l'identité finale | Compte technique déterministe, mot de passe éphémère, claims EtR et session directe ont permis l'état final `enrolled` | Contrôler la rotation automatique du jeton sur une durée supérieure à une heure |
| ETR-016 | Image Docker | Des modules de sécurité présents dans GitHub pouvaient manquer de l'image déployée | P0 | Contrôle renforcé et validé | Le Dockerfile doit citer explicitement chaque module runtime | L'image copie `health.mjs`, `device-authorization.mjs` et les composants viewer; les smoke tests vérifient leur présence et `/api/health` après construction | Conserver la liste explicite et les smoke tests sur chaque évolution de la passerelle |
| ETR-017 | Compte client | Un humain vérifié devait être contrôlé sans exiger de droits administrateur Firebase à la passerelle | P0 | Validé en production | La barrière `email_verified` était correcte, mais `auth.getUser` échouait avec `auth/insufficient-permission` avant l'autorisation du membership | Le compte est désormais relu par `accounts:lookup` avec son propre ID token; `localId`, `disabled`, `emailVerified` et `validSince` sont contrôlés. Le test humain vérifié a ensuite obtenu la remote-session et le flux VNC réel sans rôle Auth Admin | Conserver les tests humain vérifié, non vérifié, désactivé/révoqué et compte technique historique |
| ETR-018 | Règles RTDB | EtR-core ne contenait pas encore les règles `email_verified` fusionnées côté site et le workflow tentait une auto-attribution IAM | P0 | Fusionné, renforcement membership actif en PR | Les règles humaines étaient synchronisées mais les chemins d'écriture vérifiaient le rôle sans vérifier explicitement `active=true` | Les lectures exigent déjà un membership actif; le correctif ajoute la même barrière à configuration, maintenance et commandes et couvre chaque chemin par tests | Fusionner, publier les règles, relire le SHA-256 distant et tester un membership inactif |
| ETR-019 | Preuve Cloud | Le rapport de déploiement pouvait déclarer `deploy=success` sans prouver la version applicative réellement servie | P0 | Preuve complète versionnée | La sonde ARM64 prouve RTDB, WebSocket appareil 101 et `devices=1`; le viewer technique prouve le client 101 et la bannière RFB; le test humain prouve e-mail vérifié, membership, ticket, noVNC et nettoyage | `.github/deployment/etr-remote-viewer-human-live-e2e-last.json` termine avec `jobStatus=success`, `exitCode=0`, `remoteSessionStatus=200`, `websocketStatus=101`, `RFB 003.008`, puis suppression du compte et du membership | Conserver ces preuves après chaque changement d'authentification ou de tunnel distant |
| ETR-020 | Compatibilité bridge | Le Raspberry n'acceptait que `customToken` | P0 | Corrigé, déployé et validé | Une session directe sécurisée ne pouvait pas être enregistrée localement | Bridge 3.2 accepte `idToken` + `refreshToken` et le déploiement physique termine avec `enrollment_status=enrolled` | Contrôler le renouvellement après expiration du premier ID token |
| ETR-021 | Configuration Firebase Cloud | Le déploiement de la session directe exigeait un secret `FIREBASE_API_KEY` absent du dépôt EtR-core | P0 | Déployé et validé le 2026-08-02 | La clé Firebase Web est une configuration publique déjà publiée par Firebase Hosting | Lecture de `__/firebase/init.json`, validation du `projectId`, masquage de l'API key et bootstrap réussi dans le rapport physique | Conserver la découverte automatique et le contrôle de projet |
| ETR-022 | Administration globale | Le compte ORYX vérifié ouvrait `/etr-admin/` mais recevait « Erreur d’administration EtR » au premier bootstrap | P0 | Déployé et validé | Le runtime Cloud Run ne possède pas `firebaseauth.users.*` et ne peut pas s'auto-attribuer de rôle IAM; l'ancien bootstrap transformait l'accès autorisé en erreur 500 | L'adresse vérifiée inscrite dans `ETR_ADMIN_EMAILS` obtient directement le niveau write et un profil/audit RTDB, sans dépendre d'un claim. La flotte, les installations, memberships, métadonnées, enrôlements et audit restent disponibles; la liste utilisateurs se replie sur les index RTDB | Conserver le mode dégradé et séparer ultérieurement les opérations Firebase Auth privilégiées dans un service dédié |
| ETR-023 | Acquisition ADS1263 | Les CAREL et AKO étaient câblés sans driver ni interface ; les fils provisoires des NTC pouvaient être interprétés comme une température | P0 | Déployé et validé physiquement le 2026-08-02 | L'écran occupait SPI0.0/SPI0.1, GPIO17 et maintenait initialement GPIO18 bas ; le portail root perturbait aussi les droits du fichier d'état | SPI0.2/GPIO22, RESET GPIO18 haut, reset logiciel, service non privilégié et écriture atomique validés. AIN0/AIN1 publient les pressions BP/HP ; AIN2 publie la T° aspiration et AIN3 la T° ligne liquide. Tension brute visible sans résistance, température automatique avec β=3950 K après raccordement, et seuils haut/bas paramétrables dès maintenant sur chaque sonde. | Poser chaque résistance 10 kΩ 1 %, puis contrôler les deux températures avec une référence et ajuster le coefficient si nécessaire |
| ETR-024 | Navigation tactile locale | Le kiosque plein écran ne permettait pas de retrouver le bureau Linux sans clavier ; après fermeture, aucun retour tactile garanti vers EtR | P0 | Déployé et validé physiquement | Le Raspberry est utilisé sans clavier et doit rester administrable localement par la souris ou l'écran tactile | Bouton « Bureau Linux » servi, raccourci « Revenir à EtR » présent, bureau SPI actif, kiosque et Chromium actifs après l'aller-retour | Conserver ce test dédié après chaque modification du kiosque ou du bureau |
| ETR-025 | Diagnostic brut ADS1263 | Le rapport d'acquisition masquait l'octet reçu et ne distinguait pas conflit GPIO, mauvais mode SPI, vitesse ou absence physique du HAT | P0 | Validé physiquement le 2026-08-02 | Les premiers essais à zéro provenaient d'un RESET bas et d'erreurs d'exécution, pas d'un défaut démontré du HAT | La sonde finale retourne `probeExitCode=0`, détecte l'ADS1263, lit `ID=0x23`, `POWER=0x11`, `INTERFACE=0x05`, confirme la révision 3 et retrouve une télémétrie `online` avec le service actif | Conserver le mode SPI 1 et cette sonde comme contrôle de régression matériel |
| ETR-026 | Réparation automatique écran | Le workflow rapide échouait sans cause visible alors que le relais permanent était connecté | P1 | Corrigé et validé | Le diagnostic `bash -x` a identifié `ModuleNotFoundError: No module named 'firebase_bridge'` : le script utilisait `PYTHONPATH` à la racine alors que `remote_screen_agent` importe les modules top-level depuis `src`; il redémarrait aussi inutilement le bridge Firebase en auto-restart | `PYTHONPATH` inclut désormais `src`, les imports correspondent à l'exécution systemd, la réparation ne redémarre plus le bridge et le rapport final indique `repairExitCode=0`, `connected=true`, `devices=1` | Conserver cette réparation comme contrôle après toute modification du relais |
| ETR-027 | Viewer live de bout en bout | La liaison appareil 101 ne prouvait pas encore que le ticket, la page viewer, le bundle noVNC et le tunnel client transportaient réellement le protocole VNC | P0 | Validé en production le 2026-08-02 | Après `trust proxy=1`, le test live a servi `/viewer` et le bundle noVNC en 200, ouvert `/client` en 101 et reçu 12 octets contenant `RFB 003.008` | La validation humaine confirme désormais le même trajet derrière un vrai ID token vérifié et un membership viewer | Conserver comme test de régression après toute modification Cloud Run, noVNC ou VNC local |
| ETR-028 | Parcours humain vérifié | Le diagnostic technique ne prouvait pas qu'un utilisateur Firebase avec e-mail vérifié et membership pouvait ouvrir réellement l'écran | P0 | Validé en production le 2026-08-02 | Un vrai e-mail Firebase a été reçu et appliqué; le nouvel ID token contenait `email_verified=true`, le membership viewer a été créé et la passerelle a autorisé la session | Le test a terminé avec remote-session 200, viewer/noVNC 200, WebSocket 101, `RFB 003.008`, `membershipDeleted=true`, `userDeleted=true`; le Raspberry est resté connecté avec `devices=1` et `viewers=0` | Effectuer le dernier contrôle visuel depuis le compte client réel d'Emmanuel et le téléphone Samsung |
| ETR-029 | Délivrabilité e-mails | Les e-mails réels de vérification utilisent encore l'expéditeur, l'objet anglais et le lien d'action Firebase par défaut; Gmail les classe en Spam | P0 | Permission de configuration refusée; capacités IAM en audit | L'identité WIF EtR-core s'authentifie correctement mais la lecture Identity Platform retourne HTTP 403 faute de `firebaseauth.configs.get`; l'audit DNS confirme SPF Google Workspace et MX Google, sans DMARC ni réponse sur les sélecteurs DKIM usuels testés | Le workflow `projects.testIamPermissions` vérifie sans mutation les droits de lecture/mise à jour Identity Platform, de lecture/modification de politique projet et de création de rôle personnalisé; seule une empreinte du principal est conservée | Lire la preuve IAM; automatiser uniquement une attribution de lecture minimale si la politique projet le permet et si elle reste réversible, sinon préparer l'action propriétaire précise sans attribuer de rôle administrateur global |
| ETR-030 | Identité e-mail du test humain | Le test de régression utilisait encore une boîte issue d'une activité historique, sans rapport avec ORYX | P0 | Corrigé et fusionné | Le workflow utilise désormais `vars.ETR_HUMAN_E2E_EMAIL_BASE` avec repli `amotard.oryx@gmail.com`; aucune configuration Firebase, aucun droit IAM et aucun composant Raspberry n'est modifié | Une barrière de test interdit le retour d'un domaine historique et conserve le masquage des adresses et jetons | Conserver uniquement des boîtes officielles ORYX |
| ETR-031 | Rendu tactile embarqué | Le portail et le bouton Bureau Linux restaient visibles, mais le dashboard affichait une page blanche et Chromium présentait une barre French / English | P0 | Corrigé et validé physiquement | Le portail `127.0.0.1:8090` charge le dashboard `127.0.0.1:8000` dans un iframe, alors que le dashboard envoyait `frame-ancestors 'none'` | Le CSP autorise uniquement `http://127.0.0.1:8090`, le dashboard passe en 1.2.1 et le kiosque force `fr-FR` avec Translate/TranslateUI désactivés; services actifs, HTTP 200 et contrat iframe contrôlés | Conserver le contrôle physique après toute modification du kiosque ou du dashboard |
| ETR-032 | Membership inactif | Un rôle conservé avec `active=false` ne donnait plus la lecture, mais les expressions d'écriture ne vérifiaient pas encore le drapeau actif | P0 | Correctif en PR | Configuration, maintenance et commandes contrôlaient le rôle et l'e-mail vérifié, sans barrière explicite `active=true` | Les cinq chemins d'écriture humaine exigent désormais un membership actif avant l'évaluation du rôle; tests dédiés vérifient la présence et l'ordre du garde-fou | Fusionner, publier les règles RTDB et exiger une preuve de rejet d'un droit inactif |
| ETR-033 | Fabrique microSD locale | Un nouvel EtR nécessitait encore une préparation de carte externe et le clonage brut aurait dupliqué l'identité, les jetons et le runner du banc | P0 | Implémenté, validation physique à faire | Le lecteur USB du banc doit produire une carte complète depuis le bureau Linux, y compris pour un EtR sans écran | Raccourci bureau séparé de l'interface EtR, sélection limitée aux disques USB/amovibles, copie redimensionnée par fichiers, nettoyage des secrets et identités, Wi-Fi actif optionnel, nouveau hostname/clé Ed25519 au premier boot et ticket Cloud 256 bits à usage unique autorisé uniquement pour le banc connu | Créer une microSD d'essai depuis le bureau, démarrer le Raspberry Pi 3 sans écran et vérifier le réseau, le ticket consommé, le nouvel identifiant et l'absence de credentials du banc |
| ETR-034 | Session Firebase des EtR fabriqués | L'enregistrement Ed25519 par ticket usine ne suffisait pas à donner au nouvel EtR sa propre session Firebase, tandis que l'émetteur historique dépend encore de droits Firebase Auth Admin absents de Cloud Run | P0 | Correctif implémenté, CI et essai physique à faire | Une carte pouvait être clonée correctement mais rester sans `firebase-auth.json` au premier démarrage | Le ticket usine dérive un compte Firebase technique autonome via Identity Toolkit, la passerelle lie son UID à `deviceAccess`, puis le premier boot enregistre atomiquement ID token et refresh token en `0600`, conserve une preuve sans jetons et supprime le ticket seulement après succès; aucun claim ni droit Firebase Auth Admin n'est requis | Fusionner après CI, déployer la passerelle, fabriquer une carte réelle et vérifier connexion RTDB/WebSocket, renouvellement du jeton et démarrage sans écran |
| ETR-035 | Autorisation du banc microSD | Le banc est connu sous l'identité canonique `etr-0000dd7429c2`, sa session Firebase historique porte l'alias `etr-core` et son compte a été créé avant le claim `etrDevice`, ce qui provoquait encore un refus HTTP 403 avant tout effacement | P0 | Second correctif en PR | Le premier correctif autorisait l'alias, mais la route exigeait toujours strictement `etrDevice=true` alors que le vérificateur Firebase acceptait ce compte historique grâce à `deviceAccess` | La route conserve le chemin moderne par claims et, pour un compte historique, relit côté serveur `deviceAccess/<uid>`; seuls `etr-core` et l'identifiant canonique du banc sont acceptés, avec tests de refus d'un EtR terrain | Fusionner, déployer la passerelle, relancer « Préparer la carte » et vérifier l'émission du ticket avant le formatage |
| ETR-037 | Audio Bluetooth et microphone | La JBL Go 3 ne présentait pas de sink BlueZ et le microphone n’était pas exploité par EtR | P0 | Implémenté, validation physique en cours | La pile PipeWire/WirePlumber est installée, la JBL connue est associée et reconnectée automatiquement, le microphone est capturé en continu avec niveaux RMS/crête et un échantillon roulant de 5 s en RAM | Services `etr-jbl-connect` et `etr-audio-monitor`, API `/api/v1/audio`, preuve ARM64 sans archivage audio | Confirmer la connexion A2DP, identifier la source micro réelle et contrôler l’activité vocale sur le banc |
| ETR-038 | Orchestration fabrique microSD | La fenêtre, le moteur destructif, les relances et les diagnostics étaient couplés ; une fermeture ou un déploiement rendait l'état de la carte ambigu | P0 | Refactorisation fusionnée, validation physique en cours | Les essais ont montré des fenêtres fermées sans résultat final, plusieurs services provisoires et des workflows capables de relancer ou arrêter la copie | Moteur systemd indépendant, état atomique persistant, verrou exclusif, rattachement de l'interface, progression globale avec débit/ETA, annulation contrôlée, source Git figée, suppression des workflows provisoires | Réaliser un cycle complet et un test fermeture/réouverture de fenêtre |
| ETR-039 | Snapshot Git de la fabrique | Le moteur root sous `NoNewPrivileges=true` ne peut pas utiliser `runuser` pour lire le dépôt appartenant à `oryx` | P0 | Correctif en PR | Le journal physique retourne `runuser: cannot set user id: Operation not permitted` avant toute copie ; la carte reste démontée et non validée | Remplacement du changement d'identité et du clone local par `git archive` du commit courant avec `safe.directory` limité au dépôt, extraction locale sans réseau et empreinte `.etr-source-revision` | Valider la CI, déployer, relancer une fabrication unique puis confirmer le passage à `copying_root` et l'état final `ready` |
| ETR-040 | Lecteur microSD USB instable | Le lecteur SY-T18 a remis sa capacité à zéro pendant une écriture soutenue, provoquant des erreurs E/S et une carte incomplète | P0 | Nouvelle tentative prudente en PR | Le noyau a enregistré des pertes d'écriture et des changements de capacité de 31,3 Go à 0 ; l'utilisateur demande néanmoins un nouvel essai avec le même lecteur | Débit rsync limité à 2 Mio/s, exclusion des documentations et caches non nécessaires, surveillance de capacité toutes les 2 s et arrêt immédiat si le support disparaît | Effacer, reformater et réécrire une seule carte ; valider uniquement l'état final ready après contrôle et démontage |
| ETR-041 | Reprise après perte USB | L'arrêt immédiat protège la carte mais impose de recommencer toute la copie après un décrochage de quelques secondes | P0 | Implémentation en PR | Le lecteur reste sur le port USB bleu ; l'utilisateur demande une interruption momentanée et une reprise progressive | Arrêt du groupe rsync, attente 90 s du même support identifié par taille/PTUUID/PARTUUID/UUID, stabilité 5 s, fsck hors montage, remontage et reprise rsync avec fichiers partiels ; deux reprises maximum | Valider en CI, déployer sur le banc puis provoquer un seul décrochage contrôlé pendant la copie et vérifier les états paused_usb, checking_filesystem, resuming_copy et ready |
| ETR-042 | Point de montage de reprise USB | Une copie complémentaire dans un sous-répertoire pouvait mémoriser ce sous-répertoire comme cible de remontage au lieu de la racine réelle du système de fichiers | P0 | Correctif en PR | La reprise d'une copie du dépôt EtR doit remonter `rootfs` sur le point de montage `/run/.../root`, jamais dans `/home/oryx/EtR-core` | `inspect_target` lit désormais le champ TARGET réel de `findmnt`; les contrôles et démontages utilisent une correspondance exacte de point de montage avec `findmnt -M` | Valider la CI puis intégrer ce correctif avant tout essai physique de déconnexion USB |
| ETR-043 | Récupération USB avant copie | Le lecteur peut se mettre hors ligne dès l'écriture du secteur 0, avant que la reprise rsync ne puisse intervenir | P0 | Implémentation en PR | L'essai réel a échoué dans `wipefs` avec `Device offlined`, `I/O error dev sda sector 0` et aucun état de copie atteint | Le moteur réduit le délai SCSI à 15 s, limite la taille des requêtes, détecte le décrochage dans les phases effacement/partitionnement/formatage, réinitialise le même lecteur sur le même port USB, attend 90 s puis relance la préparation depuis l'effacement ; deux récupérations maximum | Valider en CI, déployer puis vérifier qu'un décrochage avant copie produit `paused_usb_setup`, `restarting_preparation` et finit par atteindre `copying_root` ou un échec terminal borné |
| ETR-044 | Identification du port USB du lecteur | La première exécution de la récupération pré-copie s'est arrêtée avant l'effacement car le lien SCSI `/sys/class/block/sda/device` ne remontait pas de nœud contenant directement `idVendor` et `idProduct` | P0 | Correctif validé physiquement | La carte et le lecteur étaient présents sur `/dev/sda`, 31,3 Go, USB `1908:0226`, mais `inspect_physical_target` retournait `lecteur USB cible non identifiable` | Repli par `udevadm info --query=path`, extraction du nœud physique depuis DEVPATH et validation dans `/sys/bus/usb/devices`; test direct et dans le sandbox systemd réel | Fusionner puis relancer le même travail pour observer la récupération bornée pendant l'effacement |
